### WARNING -- this file was generated by generate-workflows.js
name: build-test-deploy
on: pull_request_target
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      # Yes, we really want to checkout the PR
      - run: |
          if [[ -v $GH_PR_NUM ]]; then 
            git fetch origin pull/$GH_PR_NUM/head:tmp
            git checkout tmp
          fi
      - uses: actions/setup-node@v1
        with:
          node-version: '14'
      - uses: actions/cache@v2
        id: yarn-cache
        name: Cache npm deps
        with:
          path: |
           node_modules
           **/node_modules
           ~/.cache/Cypress
          key: ${{ runner.os }}-yarn-14-${{ hashFiles('yarn.lock') }}
      - run: yarn install --frozen-lockfile
        if: steps.yarn-cache.outputs.cache-hit != 'true'
      - uses: actions/cache@v2
        id: build-cache
        name: Cache dist
        with:
          path: packages/*/dist
          key: ${{ runner.os }}-dist-14-${{ hashFiles('yarn.lock') }}
      - name: Build dist
        run: yarn build && yarn build:umd
      - name: Upload dist
        uses: actions/upload-artifact@v2
        with:
          name: dist
          path: |
            packages/*/dist
            packages/react-styles/css
  lint:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v2
      # Yes, we really want to checkout the PR
      - run: |
          if [[ -v $GH_PR_NUM ]]; then 
            git fetch origin pull/$GH_PR_NUM/head:tmp
            git checkout tmp
          fi
      - uses: actions/setup-node@v1
        with:
          node-version: '14'
      - uses: actions/cache@v2
        id: yarn-cache
        name: Cache npm deps
        with:
          path: |
           node_modules
           **/node_modules
           ~/.cache/Cypress
          key: ${{ runner.os }}-yarn-14-${{ hashFiles('yarn.lock') }}
      - run: yarn install --frozen-lockfile
        if: steps.yarn-cache.outputs.cache-hit != 'true'
      - uses: actions/cache@v2
        id: lint-cache
        name: Load lint cache
        with:
          path: '.eslintcache'
          key: ${{ runner.os }}-lint-14-${{ hashFiles('yarn.lock') }}
      - name: ESLint
        run: yarn lint:ts
      - name: MDLint
        run: yarn lint:md
      - name: '@patternfly/patternfly versions match'
        run: yarn lint:versions
  test_jest:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v2
      # Yes, we really want to checkout the PR
      - run: |
          if [[ -v $GH_PR_NUM ]]; then 
            git fetch origin pull/$GH_PR_NUM/head:tmp
            git checkout tmp
          fi
      - uses: actions/setup-node@v1
        with:
          node-version: '14'
      - uses: actions/cache@v2
        id: yarn-cache
        name: Cache npm deps
        with:
          path: |
           node_modules
           **/node_modules
           ~/.cache/Cypress
          key: ${{ runner.os }}-yarn-14-${{ hashFiles('yarn.lock') }}
      - run: yarn install --frozen-lockfile
        if: steps.yarn-cache.outputs.cache-hit != 'true'
      - name: Download dist
        uses: actions/download-artifact@v2
        with:
          name: dist
      - name: PF4 Jest Tests
        run: yarn test --maxWorkers=2
  docs:
    runs-on: ubuntu-latest
    needs: build
    env:
      SURGE_LOGIN: ${{ secrets.SURGE_LOGIN }}
      SURGE_TOKEN: ${{ secrets.SURGE_TOKEN }}
      GH_PR_TOKEN: ${{ secrets.GH_PR_TOKEN }}
    steps:
      - uses: actions/checkout@v2
      # Yes, we really want to checkout the PR
      - run: |
          if [[ -v $GH_PR_NUM ]]; then 
            git fetch origin pull/$GH_PR_NUM/head:tmp
            git checkout tmp
          fi
      - uses: actions/setup-node@v1
        with:
          node-version: '14'
      - uses: actions/cache@v2
        id: yarn-cache
        name: Cache npm deps
        with:
          path: |
           node_modules
           **/node_modules
           ~/.cache/Cypress
          key: ${{ runner.os }}-yarn-14-${{ hashFiles('yarn.lock') }}
      - run: yarn install --frozen-lockfile
        if: steps.yarn-cache.outputs.cache-hit != 'true'
      - name: Download dist
        uses: actions/download-artifact@v2
        with:
          name: dist
      - uses: actions/cache@v2
        id: docs-cache
        name: Cache webpack
        with:
          path: '.cache'
          key: ${{ runner.os }}-v4-${{ hashFiles('yarn.lock') }}
      - name: Build docs
        run: yarn build:docs
      - name: Save docs
        uses: actions/upload-artifact@v2
        with:
          name: docs
          if-no-files-found: error
          path: packages/react-docs/public
      - run: node .github/upload-preview.js packages/react-docs/public
        name: Upload docs
        if: always()
  demo_app:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v2
      # Yes, we really want to checkout the PR
      - run: |
          if [[ -v $GH_PR_NUM ]]; then 
            git fetch origin pull/$GH_PR_NUM/head:tmp
            git checkout tmp
          fi
      - uses: actions/setup-node@v1
        with:
          node-version: '14'
      - uses: actions/cache@v2
        id: yarn-cache
        name: Cache npm deps
        with:
          path: |
           node_modules
           **/node_modules
           ~/.cache/Cypress
          key: ${{ runner.os }}-yarn-14-${{ hashFiles('yarn.lock') }}
      - run: yarn install --frozen-lockfile
        if: steps.yarn-cache.outputs.cache-hit != 'true'
      - name: Download dist
        uses: actions/download-artifact@v2
        with:
          name: dist
      - name: Build demo app
        run: yarn build:integration
      - name: Save build folder
        uses: actions/upload-artifact@v2
        with:
          name: demo_app
          if-no-files-found: error
          path: packages/react-docs/demo-app/public
  test_integration:
    runs-on: ubuntu-latest
    needs: demo_app
    strategy:
      fail-fast: false
      matrix:
        containers: [1, 2, 3, 4, 5]
    steps:
      - uses: actions/checkout@v2
      # Yes, we really want to checkout the PR
      - run: |
          if [[ -v $GH_PR_NUM ]]; then 
            git fetch origin pull/$GH_PR_NUM/head:tmp
            git checkout tmp
          fi
      - uses: actions/setup-node@v1
        with:
          node-version: '14'
      - uses: actions/cache@v2
        id: yarn-cache
        name: Cache npm deps
        with:
          path: |
           node_modules
           **/node_modules
           ~/.cache/Cypress
          key: ${{ runner.os }}-yarn-14-${{ hashFiles('yarn.lock') }}
      - run: yarn install --frozen-lockfile
        if: steps.yarn-cache.outputs.cache-hit != 'true'
      - name: Download the build folders
        uses: actions/download-artifact@v2
        with:
          name: demo_app
      - name: Cypress tests
        uses: cypress-io/github-action@v2
        with:
          install: false
          start: yarn serve:integration
          wait-on: 'http://localhost:3000'
          wait-on-timeout: 10
          browser: chrome
          record: true
          parallel: true
          group: 'UI - Chrome'
          spec: packages/react-integration/cypress/integration/*
  test_a11y:
    runs-on: ubuntu-latest
    needs: docs
    env:
      SURGE_LOGIN: ${{ secrets.SURGE_LOGIN }}
      SURGE_TOKEN: ${{ secrets.SURGE_TOKEN }}
      GH_PR_TOKEN: ${{ secrets.GH_PR_TOKEN }}
    steps:
      - uses: actions/checkout@v2
      # Yes, we really want to checkout the PR
      - run: |
          if [[ -v $GH_PR_NUM ]]; then 
            git fetch origin pull/$GH_PR_NUM/head:tmp
            git checkout tmp
          fi
      - uses: actions/setup-node@v1
        with:
          node-version: '14'
      - uses: actions/cache@v2
        id: yarn-cache
        name: Cache npm deps
        with:
          path: |
           node_modules
           **/node_modules
           ~/.cache/Cypress
          key: ${{ runner.os }}-yarn-14-${{ hashFiles('yarn.lock') }}
      - run: yarn install --frozen-lockfile
        if: steps.yarn-cache.outputs.cache-hit != 'true'
      - name: Download docs
        uses: actions/download-artifact@v2
        with:
          name: docs
      - name: a11y tests
        run: yarn serve & yarn test
      - name: Upload docs
        run: node .github/upload-preview.js packages/react-docs/coverage
        if: always()

