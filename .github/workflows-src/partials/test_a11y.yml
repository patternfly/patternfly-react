test_a11y:
  runs-on: ubuntu-latest
  needs: docs
  steps:
    %checkout%
    %install%
    - name: a11y tests
      run: yarn serve & yarn test
    - name: Upload docs
      run: node .github/upload-preview.js public
      if: always()

