import { type ReactNode, type HTMLProps, type FunctionComponent } from 'react';
import styles from '@patternfly/react-styles/css/{{typeDir}}/{{componentName}}';
import { css } from '@patternfly/react-styles';

interface {{ componentName }}Props extends HTMLProps<HTMLDivElement> {
  /** Content rendered inside {{ componentName }} */
  children?: ReactNode;
  /** Class to add to outer div */
  className?: string;
}

export const {{componentName}}: FunctionComponent<{{componentName}}Props> = ({
  className,
  children,
  ...props
}: {{ componentName }}Props) => {
  return (
    <div className={css(styles.{{ camelCase componentName }}, className)} {...props}>
      {children}
    </div>
  );
}
{{componentName}}.displayName = '{{componentName}}';

