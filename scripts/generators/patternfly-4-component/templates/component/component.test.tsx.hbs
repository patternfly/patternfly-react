import React from 'react';
import { render } from '@testing-library/react';
import {{componentName}} from './{{componentName}}';

test('basic', () => {
  const view = render(<{{componentName}} />);
  // Add a useful assertion here.
  expect(view).toBe(false);
});
