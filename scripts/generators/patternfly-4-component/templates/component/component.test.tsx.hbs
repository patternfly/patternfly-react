import React from 'react';
import { shallow } from 'enzyme';
import {{componentName}} from './{{componentName}}';

test('basic', () => {
  const view = shallow(<{{componentName}} />);
  // Add a useful assertion here.
  expect(view).toBe(false);
});
