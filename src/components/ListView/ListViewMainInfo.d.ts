import { SFC, ReactNode } from 'react';

export interface ListViewMainInfoProps {
  children?: ReactNode;
}

declare const ListViewMainInfo: SFC<ListViewMainInfoProps>;

export default ListViewMainInfo;
