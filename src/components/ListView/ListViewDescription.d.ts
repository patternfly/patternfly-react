import { SFC, ReactNode } from 'react';

export interface ListViewDescriptionProps {
  children?: ReactNode;
}

declare const ListViewDescription: SFC<ListViewDescriptionProps>;

export default ListViewDescription;
