{{#each icons}}
import { default as {{name}} } from './{{id}}';
{{/each}}

const iconMap = new Map([
  {{#each icons}}
  ['{{id}}', {{name}}]{{#unless @last}},{{/unless}}
  {{/each}}
]);

export {
  iconMap,
  {{#each icons}}
  {{name}}{{#unless @last}},{{/unless}}
  {{/each}}
}
