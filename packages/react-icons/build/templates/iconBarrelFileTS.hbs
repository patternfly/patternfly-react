import { IconType } from '../createIcon';

{{#each icons}}
export { default as {{name}} } from './{{id}}';
{{/each}}

export type IconMapKey =
  {{#each icons}}
  | '{{id}}'{{#if @last}};{{/if}}
  {{/each}}

export const iconMap: Map<IconMapKey, IconType>;
