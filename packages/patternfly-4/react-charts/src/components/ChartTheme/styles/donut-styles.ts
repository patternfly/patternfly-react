// TODO Replace label props with PF css when available

// Donut styles
export const DonutStyles = {
  label: {
    subTitle: {
      // Victory props only
      fill: '#bbb',
      fontSize: 14
    },
    subTitlePosition: 'center',
    title: {
      // Victory props only
      fontSize: 24
    }
  }
};
