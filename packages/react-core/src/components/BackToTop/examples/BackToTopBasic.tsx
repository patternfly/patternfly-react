import React from 'react';
import { BackToTop } from '@patternfly/react-core';

export const BackToTopBasic: React.FunctionComponent<React.PropsWithChildren<unknown>> = () => (
  <BackToTop isAlwaysVisible />
);
