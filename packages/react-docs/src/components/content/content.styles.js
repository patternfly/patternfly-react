import { StyleSheet } from '@patternfly/react-styles';
import { global_spacer_3xl as padding } from '@patternfly/react-tokens';

export default StyleSheet.create({
  content: {
    padding: padding.var
  }
});
